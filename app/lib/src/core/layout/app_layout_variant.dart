/// Visual / structural layout for the messaging shell (not platform OS).
enum AppLayoutVariant {
  /// Stacked navigation (typical phone portrait).
  compact,

  /// Master–detail split (landscape per product rule).
  split,
}
