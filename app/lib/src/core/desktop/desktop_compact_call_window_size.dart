import 'package:flutter/widgets.dart';

/// Shared compact native window size for desktop incoming + ongoing call popouts.
abstract final class DesktopCompactCallWindowSize {
  static const Size logicalSize = Size(200, 200);
}
